import React, { Component } from "react";
import { Container, Button } from './styled-app';

export default () => (
  <Container>
    <Button>Normal Button</Button>
    <Button primary>Primary Button</Button>
  </Container>
);

